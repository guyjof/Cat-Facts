export interface Fact {
    fact: string,
    length: number
    _id?: string
}

export interface Breed {
    breed: string,
    country: string,
    origin: string,
    coat: string,
    pattern: string
}